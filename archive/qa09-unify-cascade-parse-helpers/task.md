---
wait_human_start: false
wait_human_merge: false
dependencies: []
---

# Task: One splitter/one comparator each; consolidate cascade parse-helper duplication (QA-09)

## Metadata

- **Complexity:** Medium
- **Priority:** Medium
- **Status:** Ready for Handoff

## Context

Finding QA-09 (medium, code) of docs/ledgers/quality-audit.md, re-verified open by generation 3 (2026-09-04, post the qa07-verify-runner + qa10-unify-block-display-skip landings, HEAD cd18e34). Cascade parse-helper duplication family with real drift risk: (a) `src/cascade/supports.ts:27` `splitValue` is a byte-clone of the now-exported `src/layout/css.ts:1018` `splitTopLevel` — the supports.ts:25-26 comment still says "css.ts's private splitTopLevel", stale since it became exported; (b) selector-list splitter x2: `src/cascade/stylesheet.ts:135` `splitSelectors` vs `src/cascade/selector.ts:422` `parseSelectorList` (identical depth/quote/bracket walk); (c) aspect-ratio comparator x2: `src/cascade/media.ts:301` `parseRatio` + :370 cross-multiplication vs `src/cascade/phases/media-queries.ts:103-108` (re-parses via split('/')/parseFloat, adds a b===0 guard); (d) media comparator x2: `src/cascade/media.ts:326` `compareNum` vs `src/cascade/phases/media-queries.ts:133` `compare`. The container-condition grammar (parallel media-grammar with a hand-maintained Blink-correct == divergence) is a design decision — registered residual, NOT in scope this window.

## Requirements

- [ ] Re-verify QA-09 against HEAD first (the tree moved: qa07/qa10 landings touched scripts/ and block-inline.ts). Grep the four duplication pairs in src/cascade/ + src/layout/css.ts and record file:line evidence in the commit message. If disproven, record the disproof in docs/ledgers/quality-audit.md's attempt log and commit with no code change — no manufactured work, no silent drop.
- [ ] Consolidate each duplication to ONE shared helper, preserving each caller's exact behavior: (a) delete supports.ts's splitValue and import the exported splitTopLevel from css.ts; (b) one selector-list splitter — migrate stylesheet.ts:135 splitSelectors onto the shared splitter (or extract one both call); (c) one aspect-ratio comparator — merge media.ts:301/:370 and phases/media-queries.ts:103-108 into one helper that preserves the b===0 guard semantics; (d) one media comparator — merge media.ts:326 compareNum and phases/media-queries.ts:133 compare into one. Do NOT silently unify behavior that differs today; encode any intentional divergences with a why-comment per the repo comment policy.
- [ ] Do NOT attempt the container-condition dialect parameterization (the parallel media-grammar with its hand-maintained Blink-correct == divergence) in this window — register it as residual in docs/ledgers/quality-audit.md with evidence. This is one vertical slice with runnable verification; the honest no-issue completion path: helpers consolidated + residual registered = completion; do not manufacture extra refactors to inflate the diff.
- [ ] Touched areas this generation: QA-07 runner + table family (scripts/lib/runner.mjs, verify-tables.mjs, verify-tables-collapse.mjs, verify-layout-flexbox.mjs), QA-10 isBlockLevel (src/layout/block-inline.ts), timing ledgers (icu/layers/sweep/text-measure). Named gates for this slice: verify:supports / verify:aspect-ratio / verify:selectors / verify:selectors-structural / verify:media-queries / verify:media-modern / verify:custom-properties / verify:all. History lesson: empty/no-op landings are refused by the hook (94e3e04) — size the slice so the named gates can actually pass.
- [ ] Never weaken established quality to pass a gate: no tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers (ledgers change only from real script runs), no disabled verify scripts.
- [ ] Update docs/ledgers/quality-audit.md: QA-09 status + an attempt-log row with evidence. This is a chain fix task, NOT an audit: create no successor audit and no second audit chain. You may create at most 3 tasks total (all wait_human_start: false / wait_human_merge: false); important out-of-scope discoveries become tasks, lesser ones are registered in the ledger.
- [ ] Obey the loop protocol verbatim: This task is generation N of a capped recursion chain (quality-audit-N). The chain's purpose: continuously raise code/ledger/gate quality without regressing the established tests, ledgers, and tolerances. Hard rules for you and every task you create: 1. Work only inside this repository worktree. Never read, write, or cd into .orchestration/, tmp/, /tmp, or any path outside the worktree; never invoke the orch CLI from a shell; never git push; never do anything that could raise a permission prompt — the operator is unavailable. Scratch files are forbidden; if a temp file is unavoidable, put it under docs/reports/ (gitignored) and delete it before finishing. 2. Do the work and commit it. No human gates: every task you create must have wait_human_start: false and wait_human_merge: false. Leave nothing uncommitted when you finish. 3. Create at most 3 tasks total per task execution, via the create_task tool only. Exactly one must be the successor audit quality-audit-(N+1) with dependencies on the fix tasks you created this generation — unless N = 5, in which case you create no successor: the chain ends and every remaining finding gets a final disposition in this ledger instead. Fix tasks never spawn additional audit chains — exactly one audit chain exists. 4. Recursion state lives in this ledger. Before finishing: update finding statuses, attempt counts, and the touched-areas log. The successor audit's spec must embed: open findings, touched areas since the previous generation, and every failed/disproven attempt with its reason — so no attempt is repeated blind. 5. Retry cap. A finding with >=10 recorded task attempts across the chain is retired: record the retirement with evidence; never create another fix task for it. Within your own run: if an approach fails twice, switch approach once; if that fails too, record both attempts here and stop. If you cannot complete your fix, never leave the chain dead — still create the successor audit recording the blockage. 6. Re-verify first. Every created task begins by re-verifying its finding against HEAD (the tree moves between generations). If the finding is disproven, the task completes by recording the disproof (grep/oracle evidence) in this ledger and its commit message — code unchanged, no manufactured work, and no silent drops. If it is real, fix it. 7. Never weaken established quality to pass a gate. No tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers (ledgers update from real script runs only), no disabling of verify scripts. npm run build, the targeted verify gates for touched areas, and node scripts/check-charter.mjs must pass before you finish. 8. Fix tasks are one context window each — a single vertical slice with runnable verification. Prefer mechanical refactors guarded by existing gates. A finding needing a design decision is recorded in this ledger for the next audit, not turned into a speculative task. 9. Important out-of-scope discoveries must become created tasks (within the cap of 3; beyond it, register them here for the next audit with priority).

## Verification

npm run build exits 0. node scripts/check-charter.mjs exits 0. npm run verify:all exits 0 (it includes verify:supports, verify:aspect-ratio, verify:selectors, verify:selectors-structural, verify:media-queries, verify:media-modern, verify:custom-properties). Shell closure proof: `grep -n "function splitValue" src/cascade/supports.ts` empty (clone deleted); exactly one shared splitter for selector lists and exactly one shared aspect-ratio + media comparator remain across src/cascade/ + src/layout/css.ts — `grep -c "function parseRatio" src/cascade/*.ts src/cascade/phases/*.ts` and `grep -c "function compareNum\|function compare(" src/cascade/*.ts src/cascade/phases/*.ts` each at most 1.

## Prohibited Patterns

- No reads/writes/cd in .orchestration/, tmp/, /tmp, or anything outside the worktree; no orch CLI from a shell; no git push; no scratch files (unavoidable temp files under docs/reports/ only, deleted before finish) — permission prompts cannot be answered, the operator is unavailable.
- No more than 3 created tasks total; no successor audit and no second audit chain; no created task with wait_human_start or wait_human_merge true.
- No gate weakening: no tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers, no disabled verify scripts.
- No silent drops: if the finding is disproven or only partially fixable, it is recorded in the ledger with evidence — never left to vanish.
- No behavior change: each consolidated helper must preserve the exact parse/comparison semantics of every caller (e.g. the phases/media-queries.ts b===0 guard); do not unify semantics that differ today.
