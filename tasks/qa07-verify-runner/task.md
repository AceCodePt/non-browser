---
wait_human_start: false
wait_human_merge: false
dependencies: []
---

# Task: Extract scripts/lib/runner.mjs and migrate the table corpus family (QA-07)

## Metadata

- **Complexity:** High
- **Priority:** High
- **Status:** Ready for Handoff

## Context

Finding QA-07 (med-high, scripts) of docs/ledgers/quality-audit.md, re-verified open by generation 2 (2026-09-04, post the qa01 + qa02 fix landings). The verify harness is hand-rolled ~35x: scripts define a fixtures() walker, 36 copies of textRegionMask(), ~40 near-verbatim Chrome-harvest blocks, ~42 identical report tails; verify-tables.mjs vs verify-layout-flexbox.mjs are 241-line files differing in 14 lines; scripts/lib/ is nearly orphaned (expected.mjs + sweep.mjs, 6 importers). Generation 2 created this fix task; QA-14 (font-convention drift across scripts) folds into this runner work.

## Requirements

- [ ] Re-verify QA-07 against HEAD first (the tree moved: qa01 + qa02 landings touched tables.ts/block-inline.ts). Grep the current fixture-walker / textRegionMask / harvest-block / report-tail duplication across scripts/*.mjs and record file:line evidence in the commit message. If disproven, record the disproof in docs/ledgers/quality-audit.md's attempt log and commit with no code change — no manufactured work, no silent drop.
- [ ] Add a shared runner module in scripts/lib/ (e.g. runner.mjs) that consolidates the duplicated per-script pipeline: fixtures() discovery walker, textRegionMask(), the Chrome-harvest block, and the report tail. Expose the pieces the migrated scripts call so behavior is preserved exactly.
- [ ] Migrate the first batch onto the runner as the proof: scripts/verify-tables.mjs + scripts/verify-tables-collapse.mjs (add scripts/verify-layout-flexbox.mjs if it fits the window — it is the near-identical 14-line-diff twin of verify-tables). npm run verify:tables, verify:tables-collapse and verify:layout-flexbox must be green after the batch.
- [ ] One context window only: do NOT attempt to migrate all 50+ scripts. After the batch lands, register the residual (the remaining scripts, one batch per corpus family) in docs/ledgers/quality-audit.md as a per-batch roadmap for future generations. The honest no-issue completion path: runner lands + table family migrated + residual registered with evidence = completion; do not manufacture extra migrations to inflate the diff.
- [ ] Touched areas this generation: tables (tables-border-collapse + qa01/qa02 landed; tables.ts, block-inline.ts table paths, corpus/tables-collapse/), the verify harness (scripts/*.mjs, scripts/lib/), and the gates verify:tables / verify:tables-collapse / verify:layout-flexbox / verify:all. History lesson: the breaker path needed 3 attempts (first two landed empty archives before the session-idle no-op guard 94e3e04); tables-layout first archived PARTIAL — size slices so the named gates can actually pass; empty/no-op landings are refused by the hook.
- [ ] Never weaken established quality to pass a gate: no tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers (ledgers change only from real script runs), no disabled verify scripts.
- [ ] Update docs/ledgers/quality-audit.md: QA-07 status + an attempt-log row with evidence. This is a chain fix task, NOT an audit: create no successor audit and no second audit chain. You may create at most 3 tasks total (all wait_human_start: false / wait_human_merge: false); important out-of-scope discoveries become tasks, lesser ones are registered in the ledger.
- [ ] Obey the loop protocol verbatim: This task is generation N of a capped recursion chain (quality-audit-N). The chain's purpose: continuously raise code/ledger/gate quality without regressing the established tests, ledgers, and tolerances. Hard rules for you and every task you create: 1. Work only inside this repository worktree. Never read, write, or cd into .orchestration/, tmp/, /tmp, or any path outside the worktree; never invoke the orch CLI from a shell; never git push; never do anything that could raise a permission prompt — the operator is unavailable. Scratch files are forbidden; if a temp file is unavoidable, put it under docs/reports/ (gitignored) and delete it before finishing. 2. Do the work and commit it. No human gates: every task you create must have wait_human_start: false and wait_human_merge: false. Leave nothing uncommitted when you finish. 3. Create at most 3 tasks total per task execution, via the create_task tool only. Exactly one must be the successor audit quality-audit-(N+1) with dependencies on the fix tasks you created this generation — unless N = 5, in which case you create no successor: the chain ends and every remaining finding gets a final disposition in this ledger instead. Fix tasks never spawn additional audit chains — exactly one audit chain exists. 4. Recursion state lives in this ledger. Before finishing: update finding statuses, attempt counts, and the touched-areas log below. The successor audit's spec must embed: open findings, touched areas since the previous generation, and every failed/disproven attempt with its reason — so no attempt is repeated blind. 5. Retry cap. A finding with >=10 recorded task attempts across the chain is retired: record the retirement with evidence; never create another fix task for it. Within your own run: if an approach fails twice, switch approach once; if that fails too, record both attempts here and stop. If you cannot complete your fix, never leave the chain dead — still create the successor audit recording the blockage. 6. Re-verify first. Every created task begins by re-verifying its finding against HEAD (the tree moves between generations). If the finding is disproven, the task completes by recording the disproof (grep/oracle evidence) in this ledger and its commit message — code unchanged, no manufactured work, and no silent drops. If it is real, fix it. 7. Never weaken established quality to pass a gate. No tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers (ledgers update from real script runs only), no disabling of verify scripts. npm run build, the targeted verify gates for touched areas, and node scripts/check-charter.mjs must pass before you finish. 8. Fix tasks are one context window each — a single vertical slice with runnable verification. Prefer mechanical refactors guarded by existing gates. A finding needing a design decision is recorded in this ledger for the next audit, not turned into a speculative task. 9. Important out-of-scope discoveries must become created tasks (within the cap of 3; beyond it, register them here for the next audit with priority).

## Verification

npm run build exits 0. node scripts/check-charter.mjs exits 0. npm run verify:tables, npm run verify:tables-collapse, npm run verify:layout-flexbox, and npm run verify:all all exit 0. Shell closure proofs: the migrated scripts import the shared runner (grep 'from \x27./lib/\x27' scripts/verify-tables.mjs scripts/verify-tables-collapse.mjs is non-empty) and no longer define their own walker/mask (grep 'function textRegionMask' scripts/verify-tables.mjs scripts/verify-tables-collapse.mjs is empty); the fixtures()-walker and textRegionMask copy counts across scripts/*.mjs strictly decrease relative to the pre-migration count recorded in the attempt log.

## Prohibited Patterns

- No reads/writes/cd in .orchestration/, tmp/, /tmp, or anything outside the worktree; no orch CLI from a shell; no git push; no scratch files (unavoidable temp files under docs/reports/ only, deleted before finish) — permission prompts cannot be answered, the operator is unavailable.
- No more than 3 created tasks total; no successor audit and no second audit chain; no created task with wait_human_start or wait_human_merge true.
- No gate weakening: no tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers, no disabled verify scripts.
- No silent drops: if the finding is disproven or only partially fixable, it is recorded in the ledger with evidence — never left to vanish.
