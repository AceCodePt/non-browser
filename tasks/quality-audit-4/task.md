---
wait_human_start: false
wait_human_merge: false
dependencies: [qa09-unify-cascade-parse-helpers, qa12-unify-measure-seam]
---

# Task: Quality-audit recursion chain, generation 4 of 5 — full re-verification at post-fix HEAD; spawn generation-4 fixes and generation-5 audit

## Metadata

- **Complexity:** High
- **Priority:** High
- **Status:** Ready for Handoff

## Context

Anchor of the quality-audit recursion chain (generation 4 of 5). Generation 3 (2026-09-04, HEAD cd18e34) re-verified the registry at post-fix HEAD, dispositioned every row, created fix tasks qa09-unify-cascade-parse-helpers (QA-09) + qa12-unify-measure-seam (QA-12), and updated this ledger. You run AFTER those two fix tasks land (your dependencies), so QA-09 and QA-12 need re-verification against the post-fix tree.

Registry summary at gen-3 close: QA-01 registered (side channel + UA defaults fixed by qa01-break-tables-cycle; residual = the block-inline.ts:31 ↔ tables.ts:48 mutual import edge unchanged, a multi-thousand-line extraction of layoutElementBox + transitive deps — not re-attempted); QA-02 FIXED (shared src/layout/intrinsic.ts landed gen 2); QA-03 open (fix task charter-deferred-enforcement exists, gen 0, unlanded; check-charter.mjs:176-227 parses only the §11 matrix, charter.md:223 Deferred section unenforced); QA-04 open/drifted, tied to QA-03's schema (coverage-matrix.md:76-80 EMPTY claims stale vs charter §11 implemented, row 78 tables-collapse stale post landing, :112 "all 53 rows" vs 103); QA-05 open (fix task verify-gate-coverage exists, gen 0, unlanded; all 9 scripts still absent from verify:all, verify-rtl + verify-paint-fallback have zero npm entries, parity.md:24 overclaim persists); QA-06 DISPROVEN (intentional: WCAG relative luminance in paint.ts:215-222 vs full CIE XYZ-Lab in harness/deltaE.ts:43 — do not re-open on a hunch); QA-07 REGISTERED (gen-2 fix task qa07-verify-runner landed runner.mjs + table family; re-confirmed at gen-3 HEAD: textRegionMask 36→33, fixtures walker 51→48, verify:tables/collapse/flexbox PASS; residual = per-batch roadmap of 54 scripts in the ledger); QA-08 open/drifted (parity.md census lists media-queries/container-gap which FLIPPED to pass, omits legacy-removal/legacy-elements' 3 typed gaps, Latest Run 2026-08-14/17 stale, "Current count: 2" vs check-charter's 4); QA-09 open (fix task qa09-unify-cascade-parse-helpers created gen 3); QA-10 REGISTERED (gen-3 fix task qa10-unify-block-display-skip landed isBlockLevel at block-inline.ts:860, four sites migrated, enum grep empty; residual = monolith extraction style-resolve/paint-op/inline-format/block-flow + stack threading + makeStyle split); QA-11 open (fix task safari-config-fix exists, gen 0, unlanded; safari.ts:34 hardcoded /home/sagi/.local/share/fonts/HackNerdFont-Regular.ttf, chrome.ts:131 Droid Sans Japanese row unverified); QA-12 open (fix task qa12-unify-measure-seam created gen 3; pretext/index.ts:135 per-call Intl.Segmenter, skia.ts:57-64 vs 143-147 duplicated measure/hasFamily closures, dead fontMetricsKey at fontmetrics.ts:88); QA-13 open (tolerances.ts:81 dead 'exact'?'exact':'exact', media.ts:10 print claim vs evaluator, canvas/index.ts barrel omits measure-cache, cascade/index.ts omits supports); QA-14 REGISTERED (folded into QA-07; runner.mjs:24-25 owns the single FONT_FILE/FONT_FAMILY convention, env override preserved); QA-15 open (script-fallback.ts:157 LATIN_SAFE_RE \x00-\x7F; package.json has zero lint/typecheck scripts, build = tsc only, so nothing flags it).

Touched areas since gen-2 anchor (9ac5904): scripts/lib/runner.mjs (new, 286 lines) + scripts/verify-tables.mjs/verify-tables-collapse.mjs/verify-layout-flexbox.mjs reduced to ~28-line runVerify imports (QA-07); src/layout/block-inline.ts isBlockLevel predicate + 4 migrated display-skip sites (QA-10); timing ledgers icu/layers/sweep/text-measure refreshed by real runs; docs/ledgers/quality-audit.md landing notes + gen-3 dispositions.

Full attempt log (so no approach is repeated blind): QA-06 disproven during gen-1 verification (verification, not a task attempt) — paint.ts WCAG relative luminance vs deltaE.ts CIE XYZ-Lab, different formulas each correct; shared srgbToLinear curve a hygiene nit only. QA-01 qa01-break-tables-cycle gen 2 attempt 1 — (a) threaded availableInlineSize through TableLayoutInput, deleted setTableAvailableInlineSize/lastAvailableInlineSize (grep empty); (b) moved tableDefaultsFor into cascade/ua.ts:118-122 (grep empty in monolith); (c) cycle-breaking left residual: extraction of layoutElementBox + pushPaintOp/buildPieces/expandContents/FloatManager + stack globals + fieldset/inline-content children is multi-window. QA-02 qa02-unify-intrinsic-sizing gen 2 attempt 1 — shared src/layout/intrinsic.ts with IntrinsicPolicy/PieceSizingPolicy knobs preserving all five drift axes; four callers import ./intrinsic.js; dead minimum deleted; verify:all exit 0. QA-07+QA-14 qa07-verify-runner gen 2 attempt 1 — pre-migration evidence: textRegionMask in 36 scripts, fixtures() walker in 51, Chrome-harvest block in 52, verify-tables vs verify-layout-flexbox 24 diff lines; runner.mjs (286 lines) exposed fixtures/textRegionMask/harvestChrome/renderCandidate/exclusionMask/writeArtifacts/reportTail/runVerify; table family migrated, copy counts strictly down; verify:all exit 0; residual = 54-script per-batch roadmap; QA-14 folded in (single FONT_FILE/FONT_FAMILY convention at runner.mjs:24-25). QA-10 qa10-unify-block-display-skip gen 3 attempt 1 — pre-fix evidence: four lists at block-inline.ts:872/888/937/2777 with three float/position variants; isBlockLevel added+exported at :860; four sites migrated at 886/905/957/2799; enum grep empty, count=1; verify:all exit 0; residual = monolith extraction + stack threading + makeStyle split. Gen-3 re-verification confirmed both QA-07 and QA-10 registrations at HEAD (no new attempts). History lesson (from the 2026-08-17 review pair): the breaker path needed 3 attempts (first two landed empty archives before the session-idle no-op guard 94e3e04); tables-layout first archived PARTIAL before the real slice — size slices so the named gate can actually pass; empty/no-op landings are refused by the hook. Read the loop protocol in docs/ledgers/quality-audit.md §'Loop protocol' and re-read that ledger before acting.

## Requirements

- [ ] Read docs/ledgers/quality-audit.md (registry + protocol + attempt log). Run git log --oneline from the generation-3 anchor HEAD (cd18e34) and build the touched-areas list for this generation (expect the qa09-unify-cascade-parse-helpers + qa12-unify-measure-seam landings plus anything else the scheduler dispatched).
- [ ] Re-verify at HEAD, with file:line evidence, at minimum QA-09 and QA-12 (their fix tasks just landed — expect fixed, partial, or disproven; if a fix task archived via the honest no-issue path, confirm its disproof), and re-disposition every row still open at generation-3 close (QA-01, QA-03, QA-04, QA-05, QA-08, QA-11, QA-13, QA-15). QA-06 stays disproven unless new evidence emerges — do not re-open it on a hunch. Update every registry row: open / fixed / disproven / drifted-further, with evidence.
- [ ] Create at most 2 fix tasks this generation for the highest-severity verified open findings (skip any the registry marks disproven or retired). Each fix task embeds: the protocol verbatim, the re-verify-first requirement, the touched-areas list, the relevant attempt-log entries, one-context-window scope, runnable verification (build + named verify gates + check-charter), the honest no-issue completion path (record disproof in the ledger, change nothing), and wait_human_start/merge false. A fix task may create at most 3 tasks itself (successor audit excluded from that count) and never spawns a second audit chain.
- [ ] Create exactly one successor task quality-audit-5 (generation 5 of 5, wait_human_start/merge false, dependencies on the fix tasks you created this generation) whose spec embeds: the protocol verbatim, the updated registry summary, the touched-areas list, and the full attempt log (so no approach is repeated blind). The chain cap is N=5: THIS generation is the last to create a successor — generation 5 creates no successor and gives every remaining finding a final disposition in the ledger.
- [ ] Update docs/ledgers/quality-audit.md: statuses, attempt counts, touched-areas log entry for generation 4, any new findings registered with severity + evidence. Commit the ledger update. If a finding cannot be fixed this generation, it must still be dispositioned here — no silent drops.
- [ ] Obey the loop protocol verbatim: This task is generation N of a capped recursion chain (quality-audit-N). The chain's purpose: continuously raise code/ledger/gate quality without regressing the established tests, ledgers, and tolerances. Hard rules for you and every task you create: 1. Work only inside this repository worktree. Never read, write, or cd into .orchestration/, tmp/, /tmp, or any path outside the worktree; never invoke the orch CLI from a shell; never git push; never do anything that could raise a permission prompt — the operator is unavailable. Scratch files are forbidden; if a temp file is unavoidable, put it under docs/reports/ (gitignored) and delete it before finishing. 2. Do the work and commit it. No human gates: every task you create must have wait_human_start: false and wait_human_merge: false. Leave nothing uncommitted when you finish. 3. Create at most 3 tasks total per task execution, via the create_task tool only. Exactly one must be the successor audit quality-audit-(N+1) with dependencies on the fix tasks you created this generation — unless N = 5, in which case you create no successor: the chain ends and every remaining finding gets a final disposition in this ledger instead. Fix tasks never spawn additional audit chains — exactly one audit chain exists. 4. Recursion state lives in this ledger. Before finishing: update finding statuses, attempt counts, and the touched-areas log. The successor audit's spec must embed: open findings, touched areas since the previous generation, and every failed/disproven attempt with its reason — so no attempt is repeated blind. 5. Retry cap. A finding with >=10 recorded task attempts across the chain is retired: record the retirement with evidence; never create another fix task for it. Within your own run: if an approach fails twice, switch approach once; if that fails too, record both attempts here and stop. If you cannot complete your fix, never leave the chain dead — still create the successor audit recording the blockage. 6. Re-verify first. Every created task begins by re-verifying its finding against HEAD (the tree moves between generations). If the finding is disproven, the task completes by recording the disproof (grep/oracle evidence) in this ledger and its commit message — code unchanged, no manufactured work, and no silent drops. If it is real, fix it. 7. Never weaken established quality to pass a gate. No tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers (ledgers update from real script runs only), no disabling of verify scripts. npm run build, the targeted verify gates for touched areas, and node scripts/check-charter.mjs must pass before you finish. 8. Fix tasks are one context window each — a single vertical slice with runnable verification. Prefer mechanical refactors guarded by existing gates. A finding needing a design decision is recorded in this ledger for the next audit, not turned into a speculative task. 9. Important out-of-scope discoveries must become created tasks (within the cap of 3; beyond it, register them here for the next audit with priority).

## Verification

docs/ledgers/quality-audit.md updated at HEAD with generation-4 dispositions (every registry row re-verified or explicitly deferred with reason), a touched-areas entry, and an attempt-log row per attempted finding. tasks/quality-audit-5/task.md exists, is generation 5 of 5, carries the protocol verbatim, and depends on this generation's fix tasks; every created task has wait_human_start: false and wait_human_merge: false. No more than 3 tasks created total. npm run build and node scripts/check-charter.mjs exit 0 (the audit makes no product-code changes; if it touched none, the gates still run and pass).

## Prohibited Patterns

- No reads/writes/cd in .orchestration/, tmp/, /tmp, or anything outside the worktree; no orch CLI from a shell; no git push; no scratch files (temp files under docs/reports/ only, deleted before finish) — permission prompts cannot be answered, the operator is unavailable.
- No more than 3 created tasks total (fix tasks + the single successor audit); no second audit chain; no successor at all if generation N > 5; no created task with wait_human_start or wait_human_merge true.
- No product-code changes in the audit itself (it verifies, dispositions, and spawns — fixes belong to the spawned tasks); no gate weakening: no tolerance edits, no fixture deletion/skipping, no hand-edited ledger numbers, no disabled verify scripts.
- No silent drops: a finding may not vanish — it is open, fixed, disproven (with evidence), retired (>=10 attempts), or registered for the next generation, always in the ledger.
